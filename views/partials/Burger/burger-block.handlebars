<li>
	{{name}}

	<button class="change-Eat" data-id="{{id}}" data-neweat="{{devoured}}">
		{{#if devoured}}DEVOUR IT!{{else}}EAT IT!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>
